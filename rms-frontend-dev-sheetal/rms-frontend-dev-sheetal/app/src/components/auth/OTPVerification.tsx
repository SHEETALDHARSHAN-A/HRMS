// Keep a tiny no-op export to avoid breaking imports elsewhere until all references are migrated.
import React from 'react';

const OTPVerification: React.FC = () => null;

export default OTPVerification;
