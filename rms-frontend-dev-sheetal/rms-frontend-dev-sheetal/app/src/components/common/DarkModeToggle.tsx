// src/components/common/DarkModeToggle.tsx

import React from 'react';

// Dark mode has been removed; keep this component as a harmless stub so any
// existing imports won't break. It renders nothing.
const DarkModeToggle: React.FC = () => null;

export default DarkModeToggle;